{
  "name": "idp",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "packages/idp-providers/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces",
    "lint": "npm run lint --workspaces"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "jest": "^29.7.0",
    "eslint": "^9.0.0",
    "@types/node": "^22.0.0",
    "@types/jest": "^29.5.0",
    "ts-jest": "^29.1.0"
  }
}
